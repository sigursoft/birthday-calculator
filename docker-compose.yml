services:
  mariadb:
    image: mariadb:11.8.6
    environment:
      MARIADB_ROOT_PASSWORD: root
      MARIADB_DATABASE: birthdays
    volumes:
      - ./sql/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    ports:
      - "3306:3306"
